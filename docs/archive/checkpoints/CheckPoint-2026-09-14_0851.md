# Checkpoint - 2026-09-14_0851

## Context summary

Short session: confirmed the prior session's v0.8.0 features passed
manual QA, then found and committed a separate, already-complete v0.9.0
feature set that had been developed and live-tested (in the working tree
on disk, not yet committed) -- likely from a different session/window
against this same checkout. Reviewed that work's diff and CHANGELOG entry
for coherence before committing rather than assuming it was safe to
commit blind.

## Accomplishments

1. **v0.8.0 QA**: user confirmed "Generate FAQs from People Also Ask" and
   the three Select All/Deselect All checkboxes (from the 2026-09-13
   session) passed manual QA testing. A live-click integration test
   script (QTest-driven, not just the existing unit tests that call
   handler methods directly) was drafted but not run, since manual QA had
   already passed -- left in the session scratchpad, not committed.
2. **v0.9.0** (commit `7f5c8bb`), found already written on disk at
   session start (uncommitted) and reviewed/verified before committing:
   - **"YACSS Job ID (override)"** (YACSS Build tab, all three build
     types): every export previously auto-derived `job_id` purely from
     `slugify(Client Name)`, with no way to change it -- rebuilding the
     same client under an unchanged `job_id` silently overwrote
     `rr_yacss_factory`'s manifest entry for the prior build (live-found
     2026-09-13 against Happy Hooves Wellness, then Salvo Metal Works).
     New `_resolve_job_id` helper: blank keeps the existing auto-derived
     behavior, filled-in wins outright.
   - **Google Maps Embed Code now actually exports** (Diagram/
     Masspage_Silo_Local only): the field was saved/loaded but no
     `_build_*_job` method ever read it, so it was silently dropped on
     every export. New `_extract_google_maps_embed_url` pulls the real
     `src="..."` URL out of a pasted `<iframe>` snippet and sends it as
     `extra_fields.mymapsurl` -- verified live end-to-end (not just
     accepted-and-ignored) via a real one-off `cloud_stack` build/publish
     on 2026-09-14.
   - Full detail already written into `CHANGELOG.md`'s own `[0.9.0]`
     entry and `README.md` by whoever built this -- not duplicated here.
3. Full test suite: **193/193 passing** (was 185 at session start; 8 new
   cases added to `tests/test_export_job_json.py` for both v0.9.0
   features).

## Known issues

None carried into next session.

## Technical changes (files)

`main.py` (`YACSS Job ID (override)` field, `_resolve_job_id`,
`_extract_google_maps_embed_url`, both wired into all three `_build_*_job`
methods, `__version__` bumped to 0.9.0), `README.md`, `CHANGELOG.md`,
`tests/test_export_job_json.py`. Commit `7f5c8bb` on top of `abe7a75`.

## Next session priorities

1. Real client use of v0.8.0's PAA-driven FAQs and Select All checkboxes,
   and v0.9.0's Job ID override / Google Maps embed export, in an actual
   client rebuild scenario.
2. Still-open from the 2026-09-13 checkpoint: real client use of the
   Content Silo tab to validate output quality, and the still-deferred
   direct-WordPress-publish fast-follow via the confirmed `rankrocket_*`
   MCP tools.
3. If sessions continue landing uncommitted work on disk between
   sessions (this session's v0.9.0 find), consider whether that's
   intentional multi-window use worth a note in `CLAUDE.md`, or something
   to tighten up.

## Git status

Branch `main`. One commit ahead of the pre-session baseline (`abe7a75`):
`7f5c8bb` (v0.9.0), plus this checkpoint's own commit to follow. Pushed to
`origin/main` per this session's explicit request. Working tree clean.
