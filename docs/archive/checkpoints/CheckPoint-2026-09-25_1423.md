# Checkpoint - 2026-09-25_1423

Continuation of `CheckPoint-2026-09-25_1404.md` (same day). One feature
shipped, one feature request closed with no change, one working-rule added.
Version still 0.10.0 (the change is under `[Unreleased]` in CHANGELOG).

## Accomplishments

- **Diagram Content "Generate with AI" now yields at least 750 words of
  rendered content** (`f76324e`).
- **AI Model dropdown: no change needed.** Queried YACSS `GET /ai-models`
  live: `openai` has 7 models (`gpt-3.5-turbo`, `gpt-4-turbo`, `gpt-4`,
  `gpt-4o`, `gpt-4.1`, `gpt-5-mini`, `gpt-5`; account default `gpt-5`),
  `openrouter` has 56 (not configured on this account). The dropdown is
  already filled from that endpoint, so it can only show what YACSS
  supports. The user chose to keep it mirroring YACSS (no hide list) and
  ruled out any OpenAI-side dynamic lookup for now.
- **New project rule** (`23f34c0`): `CLAUDE.md` now requires presenting
  options with expected results via `AskUserQuestion` and waiting for the
  user's selection before implementing any code decision. This session's
  first attempt at Feature 2 violated it (picked a design unasked); the
  user stopped it and added the rule.
- Full gate green: ruff, black, mypy, pytest 227/227 (217 + 10 new).
  Pushed; `main` in sync with `origin/main` at `f76324e`.

## Technical changes

- `ai_content_generator.py` (v1.02): `MIN_DIAGRAM_CONTENT_WORDS = 750`,
  `DIAGRAM_CONTENT_TARGET_WORDS = 900`, `DIAGRAM_CONTENT_MAX_ATTEMPTS = 3`,
  `DIAGRAM_CONTENT_MIN_MAX_TOKENS = 6000`; new `render_spintax_min` /
  `count_rendered_words` (each `{a|b|c}` group resolved to its shortest
  option, innermost first, so the count is a lower bound on any real spin);
  `generate_diagram_content` retries a short draft with feedback and returns
  the longest draft rather than raising; output cap is
  `max(.env OPENAI_MAX_TOKENS, 6000)`; prompt asks for ~900 words over 8-12
  paragraphs.
- `main.py`: `_AIGeneratedTextDialog` takes `min_word_count` and shows a live
  "N/750 rendered words" label (red under, green at/over);
  `_generate_diagram_content` passes `MIN_DIAGRAM_CONTENT_WORDS`.
- Tests: 8 in `tests/test_ai_content_generator.py` (counter, retry/stop,
  longest-after-max, rendered-vs-raw, token floor both ways), 2 in
  `tests/test_ai_generate_buttons.py` (dialog counter, no-label default).
- Docs: README (under "YACSS Content Generation Mode") and CHANGELOG
  `[Unreleased]`.
- Decisions the user picked (all the recommended option): shortest-option
  counting; retry up to 3x keeping the longest; 6,000-token floor for this
  field.

## Known issues / next session priorities

- **Not yet verified against a real model.** Tests fake the API. Click
  Generate with AI on a real client once and confirm the counter reaches
  750; if models undershoot often, raise the target or attempts. Worst case
  is 3 API calls per click, and longer waits (the form is disabled during
  generation).
- Version not bumped (stays 0.10.0 with an `[Unreleased]` changelog entry);
  bump when the next release is cut (a user decision).
- The `gpt-5*` models reject `max_tokens` (need `max_completion_tokens`);
  `_call_openai` uses `max_tokens`, so an `OPENAI_MODEL` of `gpt-5*` in
  cloud-stack-generator's `.env` would fail. Untested here, not in scope
  today; default model is `gpt-4o-mini`.
- Carried over: use v0.10.0 image fields on a real client job; Content Silo
  real-client validation; bucket-keyword validation (`YACSS Bucket Keyword`
  unsafe characters such as underscores); optional image-host mismatch
  warning.
- Working practice: write multi-line edit scripts to a file in the
  scratchpad and run them, instead of inline `python -c`/heredocs in the
  shell (one inline command failed on quote parsing this session), and
  preserve CRLF when editing (working copies are CRLF, index is LF).

## Git status

Branch `main`, in sync with `origin/main`. HEAD before this checkpoint
commit: `f76324e`.
