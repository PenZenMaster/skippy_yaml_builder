# Checkpoint - 2026-09-25_0900

No changes to this repo since `CheckPoint-2026-09-23_1632.md` (v0.10.0,
`95c0fd6`); `main` is clean and in sync with `origin/main`. This checkpoint
only records what happened next door in `rr_yacss_factory` that touches
this project's fields.

## Context

- The new "Content Image URLs (one per line)" field's output was used for
  real in the factory (SARC job D, build `130239`): 13 of the client's own
  project photos, each passing this repo's export-time 400KB size check
  (`image_size_check.py`, run standalone against the job's hero, logo and
  content URLs -- no warnings).
- The published page confirmed the documented template quirk: `content_image_urls[0]`
  appears on every page as a fixed extra block in addition to the per-page
  image. This is worth a sentence in the builder's README field help if users
  keep asking about "double images" (the README already mentions it).
- The image list used for jobs B and C was a test set from another
  business's bucket, not the client's own. That list came from the job
  files, not from this repo's form.

## Known issues / next session priorities

Unchanged from `CheckPoint-2026-09-23_1632.md`: use the new fields on the next
real client job and eyeball the published pages once; Content Silo
real-client validation. Idea only (not built): warn when an image URL's host differs
from the client website's host.

## Git status

Branch `main`, clean, in sync with `origin/main`. HEAD before checkpoint:
`12cb232`.
