# Checkpoint - 2026-09-25_1404

Tooling-only session: ruff, black and mypy added to the QA gate. No
application behavior changed. v0.10.0 unchanged.

## Context

`CLAUDE.md` said the project had no ruff/black/mypy config and that
`pytest -q` was the whole gate. Confirmed nothing ran them anywhere (no
config, CI, hooks, or scripts), did a read-only dry run, then adopted them.

## Accomplishments

- Dry run on the 25 tracked `.py` files: ruff default set 37 findings,
  black would reformat 22 files (~1,300 lines), mypy 6 errors.
- Gate is now `ruff check --fix . && black . && mypy . && pytest -q`, all
  green on `main`: ruff clean, black clean, mypy "no issues found in 25
  source files", pytest 217/217.

## Technical changes

- `e527ed6` chore: `pyproject.toml` (new), `requirements-dev.txt` (+ruff,
  black, mypy, types-PyYAML), `CLAUDE.md` Quality gate section and README
  test instructions updated, plus six mypy fixes: `Optional[...]` on
  `main.py` `required_line_count` and `platform`, on
  `silo_content_generator.py` `child_topics`; None guards on
  `horizontalHeader()` and `QListWidget.item()` in `main.py`.
- `9abb77c` style: mechanical black + `ruff --fix` on 22 files (import
  order, unused imports). No behavior change.
- `c93826a` chore: `.git-blame-ignore-revs` listing `9abb77c`;
  `blame.ignoreRevsFile` set in this clone's local git config.
- Config choices: ruff selects only `E4`, `E7`, `E9`, `F`, `I` (ruff 0.16's
  default set is broad and mostly style opinion: `SIM`, `RUF`, `BLE`,
  `E501` are deliberately not enforced). Python target 3.13 (venv version).
  mypy default strictness with `ignore_missing_imports`; bodies of
  unannotated functions are not checked. Black and ruff honor `.gitignore`;
  mypy excludes `venv/ source/ build/ dist/ client_yaml/` explicitly.
- Tools installed into `venv/` (ruff 0.16.9, black 26.5.1, mypy 2.3.1).

## Known issues / next session priorities

- Working copies are CRLF (`core.autocrlf=true`, index is LF); a few files
  are LF on disk. Harmless to git, but scripts editing files should
  preserve the existing ending.
- On other clones run `git config blame.ignoreRevsFile
  .git-blame-ignore-revs` once (GitHub picks the file up automatically).
- Carried over: use the v0.10.0 fields on the next real client job and check
  the published pages; Content Silo real-client validation; bucket-keyword
  validation (`YACSS Bucket Keyword` unsafe characters such as underscores);
  optional idea: warn when an image URL host differs from the client website
  host. Possible follow-up: widen mypy to check untyped function bodies.

## Git status

Branch `main`. Feature commits pushed to `origin/main` (head `c93826a`
before this checkpoint commit). Checkpoint commit itself is not pushed.
